import { PipeSensorPipe } from './pipe-sensor.pipe';

describe('PipeSensorPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSensorPipe();
    expect(pipe).toBeTruthy();
  });
});
