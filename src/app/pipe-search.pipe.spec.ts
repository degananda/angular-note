import { PipeSearchPipe } from './pipe-search.pipe';

describe('PipeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
